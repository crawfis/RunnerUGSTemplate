.screen { width: 100%; height: 100%; }
.mono { -unity-font-definition: resource("Fonts/RobotoMono-Regular"); }
.hud-top-left   { position: absolute; left: 16px; top: 12px; font-size: 20px; }
.hud-top-right  { position: absolute; right: 16px; top: 12px; font-size: 20px; }
.hud-bottom-center { position: absolute; left: 50%; bottom: 16px; translate: -50% 0; }
